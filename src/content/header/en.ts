import type { HeaderContent } from ".";

const content: HeaderContent = {
  links: [
    {
      label: "Work",
      path: "/",
    },
    {
      label: "About",
      path: "/about", // don't translate this
    },
  ],
};

export default content;
